#!/bin/bash
# /usr/lib/supportconfig/plugins/grep_error_patterns

PLUG="grep_error_patterns"
CONF="/etc/ClusterTools2/supportconfig_plugins"

grep "^${PLUG}$" $CONF >/dev/null || exit 0 

# TODO make -z an option in $CONF
/usr/sbin/grep_error_patterns -z 2>&1
#
